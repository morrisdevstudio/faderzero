name: Deploy Issue Reporter Worker

on:
  push:
    branches: [main]
    paths:
      - 'cloudflare/issue-reporter/**'
      - 'scripts/run-cloudflare-checks.mjs'
      - 'scripts/validate-cloudflare-free-tier.mjs'
      - 'package.json'
      - 'package-lock.json'

permissions:
  contents: read

jobs:
  deploy:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: npm run check:cloudflare:costs
      - run: npm run typecheck:issue-reporter
      - run: npm run test:issue-reporter
      - run: npx wrangler@4.118.0 types --check --config cloudflare/issue-reporter/wrangler.jsonc cloudflare/issue-reporter/worker-configuration.d.ts
      - name: Validate Cloudflare deploy credentials
        run: |
          test -n "$CLOUDFLARE_API_TOKEN"
          test -n "$CLOUDFLARE_ACCOUNT_ID"
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ vars.CLOUDFLARE_ACCOUNT_ID }}
      - name: Validate Worker GitHub secret
        run: npx wrangler@4.118.0 secret list --config cloudflare/issue-reporter/wrangler.jsonc | grep -q 'GITHUB_TOKEN'
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ vars.CLOUDFLARE_ACCOUNT_ID }}
      - run: npx wrangler@4.118.0 deploy --config cloudflare/issue-reporter/wrangler.jsonc
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ vars.CLOUDFLARE_ACCOUNT_ID }}
